# {{ name }}

Project {{ project_id }}, managed by the lab platform. Generated by `lab init` on {{ created }}.

## Layout

- `lab.yaml` — repository manifest, validated by `lab validate`
- `experiments/` — experiment specifications ({{ experiment_id }} is a starter)
- `src/{{ package }}/` — model code (`python -m {{ package }}.run --config configs/smoke.yaml`)
- `configs/` — run configurations
- `results/` — output directory (declared in `lab.yaml`)

## Commands

```bash
lab validate           # check lab.yaml and experiments/
lab inspect            # summarize this workspace
PYTHONPATH=src pytest  # run this project's own tests
```

`lab build`, `lab test`, and `lab run` arrive with platform Milestone 2.
