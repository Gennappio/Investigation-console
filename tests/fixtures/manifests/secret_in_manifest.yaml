apiVersion: lab/v1
kind: Repository

metadata:
  name: leaky-repo
  owners:
    - anna.rossi

spec:
  project: PRJ-000001

  runtime:
    type: python
    version: "3.12"

  commands:
    run:
      - python
      - -m
      - leaky.run
      - --registry-token
      - ghp_0123456789abcdefghijklmnopqrstuvwxyz

  outputs:
    directory: results

  reporting:
    template: default
